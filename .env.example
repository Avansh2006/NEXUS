POSTGRES_DB=nexus
POSTGRES_USER=nexus
POSTGRES_PASSWORD=replace-with-a-local-password
FRONTEND_ORIGIN=http://localhost:8080
# Generate JWT and BCrypt settings using scripts/setup_credentials.py.
# Never use test credentials in a public deployment.
